/**
 * 
 * @param {*} param 
 */
function login (param) {
    param.split("&")
}

// based on the login method get the parameters and if they are equal to given parameters return true
function login (param) { 
    var params = param.split("&")
}
